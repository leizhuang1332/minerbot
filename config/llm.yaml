# LLM 配置文件
model_name: "minimax/MiniMax-M2.5"

# 可选：自定义端点（覆盖默认值）
base_url: ""

# 可选：API Key 环境变量名（敏感信息通过环境变量管理）
# 格式: $ENV_VAR 表示从环境变量读取
api_key: "$MINIMAX_API_KEY"

model_params:
  temperature: 0.7
  max_tokens: 4096
  timeout: 60
